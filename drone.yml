kind: pipeline
type: docker
name: default

steps:

- name: build
  image: python
  commands:
    - "go python2 ./main.py"
  #  RUN apt update
  #RUN apt install python2 -y

#WORKDIR /usr/app/src
#COPY main.py ./
#CMD ["python2 " ,"./main.py"]


- name: publish  
  image: plugins/docker
  settings:
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password
    repo: rlachhman/myrepo
    auto_tag: true
    auto_tag_suffix: drone-build
